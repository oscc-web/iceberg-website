{
    "name": "documentation",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "start": "export NODE_OPTIONS=--openssl-legacy-provider && docusaurus start",
        "build": "export NODE_OPTIONS=--openssl-legacy-provider && docusaurus build",
        "swizzle": "export NODE_OPTIONS=--openssl-legacy-provider && docusaurus swizzle",
        "deploy": "export NODE_OPTIONS=--openssl-legacy-provider && docusaurus deploy"
    },
    "dependencies": {
        "@docusaurus/core": "^2.0.0-alpha.50",
        "@docusaurus/preset-classic": "^2.0.0-alpha.50",
        "classnames": "^2.2.6",
        "mermaid": "^8.5.0",
        "react": "^16.8.4",
        "react-dom": "^16.8.4",
        "react-medium-image-zoom": "^4.3.0"
    },
    "browserslist": {
        "production": [
            ">0.2%",
            "not dead",
            "not op_mini all"
        ],
        "development": [
            "last 1 chrome version",
            "last 1 firefox version",
            "last 1 safari version"
        ]
    }
}
