{
    "name": "iceberg-website",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "start": "docusaurus start",
        "build": "docusaurus build",
        "swizzle": "docusaurus swizzle",
        "tar": "npm run build && tar -czvf build.tar.gz -C build .",
        "deploy-pre": "npm run tar; scp build.tar.gz iceberg@97.74.84.46:~",
        "deploy-aft": "ssh iceberg@97.74.84.46 'rm -rf ~/iceberg-deploy && mkdir -p ~/iceberg-deploy && tar -xzvf ~/build.tar.gz -C ~/iceberg-deploy'",
        "deploy": "npm run deploy-pre && npm run deploy-aft"
    },
    "dependencies": {
        "@docsearch/js": "^3.5.2",
        "@docusaurus/core": "^2.0.0-alpha.50",
        "@docusaurus/preset-classic": "^2.0.0-alpha.50",
        "classnames": "^2.2.6",
        "mermaid": "^8.5.0",
        "react": "^16.8.4",
        "react-dom": "^16.8.4",
        "react-medium-image-zoom": "^4.3.0"
    },
    "browserslist": {
        "production": [
            ">0.2%",
            "not dead",
            "not op_mini all"
        ],
        "development": [
            "last 1 chrome version",
            "last 1 firefox version",
            "last 1 safari version"
        ]
    }
}
